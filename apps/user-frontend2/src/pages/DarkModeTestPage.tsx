import React from "react";
import { DarkModeTest } from "@/components/dark-mode-test";

/**
 * Dark Mode Test Page
 * This page is used to test all UI components in both light and dark themes
 * to ensure proper dark mode compatibility
 */
export default function DarkModeTestPage() {
  return <DarkModeTest />;
}