model University {
  id       String    @id @default(uuid())
  name     String
  uid      Int       @unique
  users    User[]
  courses  Course[]
  hostels  Hostel[]
  notices  Notice[]
  @@map("universities")
}