model Semester {
  id       String    @id @default(uuid())
  number   Int
  code     String    @unique
  courseId String
  course   Course    @relation("SemesterCourse", fields: [courseId], references: [id])
  subjects Subject[] @relation("SemesterSubjects")
  exams    Exam[]
}
